#soon ...
#sudo: false
language: python
python:
    - "2.7"
env:
    - CKANVERSION=inventory
    - PG_VERSION=9
install:
    - bash bin/travis-build.bash
    - pip install coveralls
script: sh bin/travis-run.sh
